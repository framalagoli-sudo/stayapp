node_modules/
.env
# Credenziali del progetto di prova per il ripristino: stanno solo sul disco di
# chi fa la prova, mai in git. Il file lo crea a mano chi possiede l'account.
.env.ripristino
dist/
.DS_Store

.vercel

# Vecchi progetti dismessi (frontend Vite + backend Express, Railway spento):
# rimossi dal repo. Ignorati per evitare che un dev server locale ne rigeneri
# la cache e li re-inserisca in git. Il codice vivo e' in client-next/.
/client/
/server/

# Screenshot delle sonde diagnostiche (usa e getta)
tests/*.png

# Gli archivi scaricati da R2 sono il database dei clienti in chiaro:
# non entrano mai in git, nemmeno per sbaglio con un "git add -A".
backup/
*.json.gz

# Il file temporaneo con la chiave Stripe live: non entra mai in git.
chiave-live.txt

client-next/dev.log
